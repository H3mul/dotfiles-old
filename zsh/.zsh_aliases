
alias l='ls -lFh'
alias la='ls -lAFh'
alias lr='ls -lRAFh'
alias lt='ls -ltFh'
alias ll='ls -l'
alias ld='ls -ld .*'

alias h='history'
alias e='exit'

calc () {
    cmd="print($@)"
    python -c $cmd
}

ds () {
    du -h -d 1 $@ | sort -hr
}
